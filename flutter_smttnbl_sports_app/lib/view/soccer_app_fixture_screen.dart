import 'package:flutter/material.dart';

class SoccerAppFixtureScreen extends StatelessWidget {
  const SoccerAppFixtureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
